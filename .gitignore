# Gradle / Android build
.gradle/
build/
app/build/
local.properties
*.iml
.idea/
captures/
.cxx/

# Keep the demo signing material out of accidental edits, but it IS committed
# intentionally so the project builds a signed release APK out of the box.
# (see README — replace before any real distribution)

# OS / editor noise
.DS_Store
*.log

# Web design system (design-sync): generated build output + deps are rebuilt
# from source via cfg.buildCmd; committed inputs are the src/ and .design-sync/.
design-system/node_modules/
design-system/dist/

# design-sync staged scripts, generated bundle, machine-local cache/state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
